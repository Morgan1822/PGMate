import SwiftUI

struct MaintenanceBoardView: View {
    var body: some View {
        NavigationStack {
            Text("Maintenance")
                .navigationTitle("Maintenance")
        }
    }
}
